"""GUI theme styles.

Theme styling is applied in main_window.apply_appearance_settings() using
get_theme_colors(). This module is kept for potential future use (e.g. shared
stylesheet fragments).
"""
